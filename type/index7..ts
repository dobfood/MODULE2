export class Book{
    title:string;
    author:string;
    already:boolean;
    constructor(title:string, author:string, already:boolean) {
        this.title=title;
        this.author=author;
        this.already=already;

    }
    
}